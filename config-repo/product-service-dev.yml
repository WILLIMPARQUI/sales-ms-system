message: Configuración externa cargada desde Config Server para product-service
server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3320/sales_products_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

message: Configuración externa cargada desde Config Server para product-service