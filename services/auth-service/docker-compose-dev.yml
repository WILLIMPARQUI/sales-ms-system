services:
  mysql-auth:
    image: mysql:8.0
    container_name: mysql-auth
    restart: always
    ports:
      - "3316:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: sales_auth_db
    volumes:
      - mysql-auth-data:/var/lib/mysql

volumes:
  mysql-auth-data: