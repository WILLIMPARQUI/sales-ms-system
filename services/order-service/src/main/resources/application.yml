server:
  port: 8085

spring:
  application:
    name: order-service
  config:
    import: optional:configserver:http://localhost:7071
  datasource:
    url: jdbc:mysql://localhost:3319/sales_orders_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7081/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info